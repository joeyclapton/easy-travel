import styled from "styled-components";

const Styles = {
    container: styled.ol`
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 16px;
        padding: 0;
    `
};

export default Styles;
